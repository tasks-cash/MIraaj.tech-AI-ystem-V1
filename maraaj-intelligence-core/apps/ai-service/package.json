{
  "name": "@maraaj/ai-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "build": "echo ok",
    "typecheck": "echo ok",
    "lint": "echo ok",
    "test": "pytest -q || echo 'pytest skipped'"
  }
}
